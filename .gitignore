# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/

# AutoGluon / model artifacts (runtime, never commit)
artifacts/
*.pkl
*.ckpt
*.pt
*.pth
*.onnx
AutogluonModels/

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Local env
.env
.env.local

# Production token table — real bearer tokens live here, never commit.
# A template (deploy/tokens.example.json) is committed as documentation.
deploy/tokens.json
# Production env — contains MCP_DOWNLOAD_SIGNING_KEY (real HMAC key).
# Template (deploy/env.example) is committed as documentation.
deploy/env

# Claude Code session state
.claude/
